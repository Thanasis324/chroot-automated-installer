initd (EdwardLab/initd): Replaces systemd to manage background services (D-Bus, audio daemons) and handle PID 1 process reaping.



bwrap-shim

+ SUID Root: A small

wrapper script that strips the

--unshare-user,

--unshare-pid, and

--unshare-ipc

flags before passing

bwrap. It lets Flatpak run





commands to

using only the kernel's working Mount

Namespaces without crashing on missing User/PID namespaces.

Host Mount Propagation (rshared / rslave ): Host-level flags that allow nested mount creation inside the chroot.

FLATPAK_BWRAP Environment Variable: Points Flatpak directly to the shim instead of the default bwrap binary